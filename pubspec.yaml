name: absolutely_digital_test
description: A new Flutter project.


publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"


dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0
  stacked: ^2.1.8
  get_it: ^7.2.0
  get:
  injectable: ^1.4.1
  curved_navigation_bar: ^1.0.1
  flutter_svg:
  page_transition: ^2.0.2


flutter:

  uses-material-design: true

  assets:
    - assets/
